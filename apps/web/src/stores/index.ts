/**
 * Stores barrel export — signal-based state management.
 * SolidJS signals provide fine-grained reactivity without re-renders.
 */

export { authStore } from "./auth.js";
export { projectStore } from "./projects.js";
export { uiStore } from "./ui.js";
